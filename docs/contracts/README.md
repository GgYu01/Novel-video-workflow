# Contracts

Store schema and interface contracts here for the current production truth set:
- `Job`
- `SourceDocument`
- `StorySpec`
- `ShotPlan`
- `AssetManifest`
- `ReviewCase`
- `PolicyDecision`
- `OutputPackage`

Keep this index aligned with `src/av_workflow/contracts/models.py` and the architecture documents whenever a new immutable contract is introduced.
